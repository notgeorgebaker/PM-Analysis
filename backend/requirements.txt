# PM-Analysis backend — "Lightroom for VMD"
# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
httpx>=0.27

# Science stack
numpy>=1.24
scipy>=1.10
MDAnalysis>=2.6        # selections, distances, RMSD/RMSF, HOLE2 wrapper
freesasa>=2.2          # solvent accessible surface area

# Hardware mapping / compute allocation
psutil>=5.9            # CPU/memory detection
threadpoolctl>=3.1     # cap BLAS/OpenMP threads to the chosen core count

# Optional accelerators (auto-detected if present):
#   cupy-cuda12x       # GPU array maths for large distance/contact kernels
#   nvidia-ml-py       # richer NVIDIA GPU telemetry (pynvml)
# Optional (extra SASA / trajectory tooling): mdtraj>=1.9
